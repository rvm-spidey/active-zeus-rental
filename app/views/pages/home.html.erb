<!--
<% if user_signed_in? %>
    <h1>Welcome <%= current_user.first_name %>  <%= current_user.last_name %> </h1>

    <h2>Admin ? <%= current_user.admin? %> </h2>

    <p> <%= current_user.last_name %> |
        <%= current_user.address %>   |
        <%= current_user.email %>  |
        <%= current_user.district %>
    </p>
<% else %>
    <h1>Please sign up/in </h1>
<% end %>
-->

<div class="banner">
  <h1>Active Zeus</h1>
  <% if user_signed_in? %>
    Welcome, back <%= current_user.last_name %> <button>  Discover Equipments </button>
    <% else  %>
    <button> Sign Up </button>
  <% end %>
</div>
